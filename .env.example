# Copy this file to `.env.local` and fill in your own values.
#
# ADMIN_PASSPHRASE gates the hidden content console. It is checked on the
# server (see src/app/api/unlock/route.ts), so it never reaches the browser
# bundle. Set the same variable in Vercel for production.

ADMIN_PASSPHRASE=change-me
